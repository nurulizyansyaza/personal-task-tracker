# ============================================
# Frontend Production - us-east-1
# ============================================
# Frontend + Nginx (proxies API to ap-southeast-1)
# Differences from staging:
#   - container names use "-production" suffix
#   - ECR image tag is "production"
# ============================================

services:
  frontend:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.us-east-1.amazonaws.com/ptt-frontend:production
    container_name: ptt-frontend-production
    restart: always
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}

  nginx:
    image: nginx:alpine
    container_name: ptt-nginx-production
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf.template:/etc/nginx/templates/default.conf.template:ro
    environment:
      API_HOST: ${API_HOST:?API_HOST required}
    depends_on:
      - frontend
