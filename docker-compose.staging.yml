# ============================================
# Staging Docker Compose
# ============================================
# Pulls images from ECR, connects to RDS
# Deployed automatically on push to staging branch
# ============================================

services:
  # Redis Cache (runs on EC2)
  redis:
    image: redis:7-alpine
    container_name: ptt-redis-staging
    restart: always
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NestJS API (from ECR)
  api:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/ptt-api:staging
    container_name: ptt-api-staging
    restart: always
    environment:
      NODE_ENV: staging
      PORT: 3000
      DB_HOST: ${DB_HOST}
      DB_PORT: 3306
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CORS_ORIGIN: ${CORS_ORIGIN}
    depends_on:
      redis:
        condition: service_healthy

  # NextJS Frontend (from ECR)
  frontend:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/ptt-frontend:staging
    container_name: ptt-frontend-staging
    restart: always
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: ptt-nginx-staging
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
      - frontend

volumes:
  redis_data:
