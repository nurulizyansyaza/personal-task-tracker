# Frontend Homelab Environment Variables
# Copy to .env on the homelab server (Frontend stack)
# Note: NEXT_PUBLIC_* vars are baked at Docker build time, not runtime.

# GitHub Secrets for CI/CD:
#   STAGING_API_URL=/staging/personal-task-tracker/api
#   PRODUCTION_API_URL=/personal-task-tracker/api

# Nginx (docker-compose.nginx.yml)
SERVER_NAME=nurulizyansyaza.com
